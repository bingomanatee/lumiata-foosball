'use strict';

describe('Directive: playersGraph', function () {

  // load the directive's module
  beforeEach(module('lumiataFoosballApp'));

  var
    scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

  it('has no tests', inject(function () {
  }));
});
